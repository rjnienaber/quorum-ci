language: java
sudo: false

jdk:
  - oraclejdk7

os:
  - linux

env:
  global:
    - CI_HOME=`pwd`
    - CLASSPATH=$CLASSPATH:$CI_HOME/Quorum3/Run/*

script: cd $CI_HOME/Quorum3 && ./build.sh

notifications:
  email: false

branches:
  only:
    - master